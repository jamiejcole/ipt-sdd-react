import React from 'react';
import SearchElement from './searchElement';

const QueryResult = (props) => {
    return (
       <div>
            <SearchElement width={props.width} />

       </div>
    );
}

export default QueryResult